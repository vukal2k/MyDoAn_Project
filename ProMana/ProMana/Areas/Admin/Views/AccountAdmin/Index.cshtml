@model IEnumerable<DTO.UserInfo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="container">
    <table class="table table-bordered UseDataTables">
        <thead>
            <tr>
                <th>
                    Username
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FullName)
                </th>
                <th>
                    Current Job
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Company)
                </th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CurrentJob)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Company)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.UserName }) |
                        <a href="@Url.Action("Delete","AccountAdmin",new { id = item.UserName})" onclick="return confirm('Are u sure? Delete @item.UserName')">Delete</a>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>
@section Scripts{
    <script>
        $(document).ready(() => {
            $("#sidebar5").addClass("active");
        })
    </script>
}