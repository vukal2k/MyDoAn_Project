@{
    var errors = (List<string>)ViewBag.Errors;
    if (errors != null)
    {
        <ul>
            @foreach (var item in errors)
            {
                <li class="text-danger">@item</li>
            }
        </ul>
    }
}
