@model IEnumerable<DTO.Post>

@foreach (var item in Model)
{
    <a href="~/Blog/DetailPost/@item.PostId">
        <div class="item d-flex align-items-center">
            <div class="title">
                <strong>@item.Title</strong>
            </div>
        </div>
    </a>
}